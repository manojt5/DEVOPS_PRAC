apiVersion: v1
kind: Pod
metadata:
  name: database
  namespace: html-app
  labels:
    app: database
spec:
  containers:
  - name: postgres-db
    image: postgres:15-alpine
    env:
    - name: POSTGRES_DB
      value: myapp
    - name: POSTGRES_USER
      value: appuser
    - name: POSTGRES_PASSWORD
      value: secret123
    volumeMounts:
    - name: postgres-data
      mountPath: /var/lib/postgresql/data
  volumes:
  - name: postgres-data
    emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: database
  namespace: html-app
spec:
  selector:
    app: database
  ports:
  - port: 5432
    targetPort: 5432
  type: ClusterIP